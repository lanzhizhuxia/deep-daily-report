# News source whitelist (consumed by the news_6551 collector when enabled).
#
# daily_call_budget: documentation-only cap on API calls (runtime limit lives
#                    in code; keep this in sync for readability).
# sources: list of upstream feeds. code matches the upstream API's engineTypes
#          value exactly (case-sensitive). enabled=false skips a source without
#          removing it from the config.

daily_call_budget: 8

sources: []
