{
  "_comment": "Twitter KOL whitelist (used by twitter collectors). Each entry: {handle, group, note}. Handles are case-insensitive. Groups are user-defined lanes that downstream filters can key off.",
  "kols": []
}
