# Active systems the LLM should be aware of when writing the digest.
# Surfaced via config._load_active_systems() into writing prompts so the digest
# mentions (or side-steps) systems you care about.
#
# Each entry: {name, detail}. Leave systems empty for a generic digest.

systems: []
